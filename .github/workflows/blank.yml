name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run a one-line script
      run: echo Hello, chetan!
    - name: Run a multi-line script
      run: |
        echo I can defit the code more,
        echo test, very simple ND accurate.
